// GET /clubs
    // check for user
    // search db for all clubs
    // render clubs-view with clubs arr

// POST /joinClub/:clubId
    // get currUser
        // search for club with clubId and if found, add to the users list of clubs!
        
// POST /leaveClub/:clubId
    // get currUser
        // search for club with clubId and if found, remove from the users list of clubs!
