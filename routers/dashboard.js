
// // DASHBOARD ROUTER: contains all the routes needed for member dashboard actions.
// // Dashboard : GET (move into its own file later)

// const dashboard = require('express').Router();
// const controller = require('../controllers/dashboard');

// // GET index

// // GET /dashboard
// dashboard.post('/dashboard', (req,res) => {
//     const userId = req.body.userId;
//     controller.loadDashboard(userId).then((user) => {
        
//     }).catch(err => {

//     });
// });


// module.exports = dashboard;