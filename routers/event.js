// GET /clubName/:eventId
    // get currUser
    // if member
        // find the specific event from the club name
        // render `event-member-view` passing in event obj
    // if club Leader of that event (if they're a leader and they own that club)
    // they should see it just like regular people but they should have an edit/delete option.

    